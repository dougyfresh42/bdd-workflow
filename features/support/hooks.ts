/**
 * Cucumber hooks for bdd-workflow's own tests.
 */

import { Before, After } from '@cucumber/cucumber';

/**
 * Run before each scenario.
 */
Before(function () {
  // Set up fixtures here
});

/**
 * Run after each scenario.
 */
After(function () {
  // Clean up here
});
