# Project Context

> Auto-generated by `npx bdd-workflow context`. Do not edit manually.
> Last updated: 2026-04-06T05:16:24.070Z

## Overview

Agentic BDD development workflow framework for TypeScript projects

## Directory Structure

```
features/
  check.feature
  context.feature
  docs.feature
  init.feature
  learn.feature
  scaffold-bdd-workflow-agent.feature
  scaffold-phase2.feature
  specs.feature
  update.feature
src/
  commands/
    check.ts
    context.ts
    docs.ts
    init.ts
    learn.ts
    specs.ts
    update.ts
  generators/
    context.ts
    docs.ts
    specs.ts
    structure.ts
  learn/
    index.ts
    promote.ts
  parsers/
    gherkin.ts
    jsdoc.ts
    typescript.ts
  scaffold/
    templates/
      features/
        support/
          hooks.ts
          steps
          world.ts
      src/
        index.ts
      CONTEXT.md
      SPECS.md
      bdd-workflow.config.ts
      cucumber.js
      opencode.json
      package.json
      tsconfig.json
      typedoc.json
    frontmatter.ts
    index.ts
    manifest.ts
    update.ts
  cli.ts
  config.ts
  index.ts
```

## Modules

| File | Description |
|------|-------------|
| `src/cli.ts` | CLI entry point for bdd-workflow. |
| `src/commands/check.ts` | Implements the `bdd-workflow check` CLI subcommand. Runs the project's full verification suite — type-check followed by the Cucumber test suite — and exits non-zero if either step fails. Intended as the canonical pre-review gate referenced by the bdd-workflow skill and command files. Does NOT run the build — type-checking is performed via `npx tsc --noEmit` only. / |
| `src/commands/context.ts` | CLI command wiring for `bdd-workflow context`. Parses CLI options, loads the project configuration, and delegates to `generateContext`. Does NOT contain generation logic — that lives in src/generators/context.ts. / |
| `src/commands/docs.ts` | CLI command wiring for `bdd-workflow docs`. Parses CLI options, loads the project configuration, and delegates to `generateDocs`. Supports an optional `--format` flag to switch between markdown (default) and HTML output. Does NOT contain generation logic — that lives in src/generators/docs.ts. / |
| `src/commands/init.ts` | The init subcommand for bdd-workflow. |
| `src/commands/learn.ts` | CLI command wiring for `bdd-workflow learn`. Provides two subcommands: `list` (prints a formatted table of all learning entries) and `promote` (creates GitHub issues from unpromoted learnings via the `gh` CLI). Does NOT contain parsing or promotion logic — that lives in src/learn/index.ts and src/learn/promote.ts. / |
| `src/commands/specs.ts` | CLI command wiring for `bdd-workflow specs`. Parses CLI options, loads the project configuration, and delegates to `generateSpecs`. Supports an optional `--output` flag to override the default SPECS.md path. Does NOT contain generation logic — that lives in src/generators/specs.ts. / |
| `src/commands/update.ts` | Implements the `bdd-workflow update` CLI subcommand. Responsible for refreshing framework-owned scaffold files (agents, commands, skills, and templates) in an existing initialized project to match the current version of the bdd-workflow package. Does NOT touch user-owned files (source code, feature files, CONTEXT.md, SPECS.md, package.json, tsconfig.json, or any file not tracked in the scaffold templates' framework layer). Produces a human-readable diff summary showing updated, identical, and user-modified files. / |
| `src/config.ts` | Configuration types, defineConfig function, and loadConfig loader. |
| `src/generators/context.ts` | Orchestrates assembly of CONTEXT.md by collecting output from the structure, jsdoc, gherkin, and typescript parsers and writing the result to the configured output file. Sections are conditionally included based on config.context.sections flags. Does NOT perform any parsing itself — delegates to the parsers/ modules. / |
| `src/generators/docs.ts` | Generates API documentation from JSDoc comments using TypeDoc's programmatic Node API. Reads optional `typedoc.json` from the project root and merges sensible defaults from `bdd-workflow.config.ts`. Outputs markdown files (via typedoc-plugin-markdown) or HTML to the configured output directory. Does NOT invoke TypeDoc as a shell command — uses Application.bootstrapWithPlugins so output can be tested and errors surfaced clearly. / |
| `src/generators/specs.ts` | Assembles SPECS.md — a human-readable behavioral specification document derived from all Gherkin `.feature` files in the project. Reuses the `parseFeatureFilesDetailed` parser from src/parsers/gherkin.ts, formats full step text (keyword + text), feature descriptions, and scenario tags, and appends a summary table. Does NOT abbreviate scenario outlines to individual example rows — shows the outline template with `<placeholder>` syntax instead. Does NOT write CONTEXT.md — that is handled by src/generators/context.ts. / |
| `src/generators/structure.ts` | Generates an indented directory tree string from the file paths matched by the configured include/exclude patterns. Uses `glob` for file collection. Does NOT depend on the system `tree` binary — the tree is rendered in-process. / |
| `src/index.ts` |  |
| `src/learn/index.ts` | Parses, lists, and updates learning entry files stored in `.opencode/learnings/`. Learning entries are markdown files with YAML frontmatter conforming to the LearningEntry schema. Provides `listLearnings` to enumerate all entries and `markAsPromoted` to update frontmatter after a GitHub issue is created. Does NOT perform GitHub API calls — that is handled by src/learn/promote.ts. Does NOT write new learning entries — that is the responsibility of the /learn OpenCode command. / |
| `src/learn/promote.ts` | Promotes unpromoted learning entries to GitHub issues by invoking the `gh` CLI. Filters to entries with `status: new` and `promoted: false`, constructs a structured issue body from each entry's sections, and calls `markAsPromoted` to update frontmatter after successful creation. Supports a `--dry-run` mode that prints issue content without creating anything. Checks for `gh` CLI availability before running and exits with a clear error if not found. Does NOT parse learning files — that is handled by src/learn/index.ts. / |
| `src/parsers/gherkin.ts` | Parses Gherkin `.feature` files using the official `@cucumber/gherkin` package and extracts Feature names and Scenario names into a structured summary. Handles Rules, Scenario Outlines, and Examples tables. Does NOT execute scenarios — parsing only. Extended in Phase 4 with `parseFeatureFilesDetailed` which additionally extracts per-step keyword and text, feature descriptions, feature-level tags, and scenario-level tags. / |
| `src/parsers/jsdoc.ts` | Extracts file-level JSDoc comments from TypeScript source files using the TypeScript compiler API. Targets the leading `/** ... *\/` block comment of each file (the `@module` description). Does NOT use regex — the TS compiler handles edge cases correctly. Files without a file-level JSDoc comment are silently skipped. / |
| `src/parsers/typescript.ts` | Extracts exported function and class signatures from TypeScript source files using the TypeScript compiler API (ts.Program + TypeChecker). Returns human-readable signature strings suitable for the Public API section of CONTEXT.md. Does NOT produce HTML or markdown links — plain text signatures only. / |
| `src/scaffold/frontmatter.ts` | Utilities for parsing, merging, and serializing YAML frontmatter in OpenCode agent and command markdown files. Enables `update` to refresh framework-owned content (body text, structural frontmatter keys like `description`, `mode`, `agent`, `permission`) while preserving user-owned keys (`model`, `temperature`) that users legitimately customize to match their LLM provider. Uses the already-present `gray-matter` dependency for frontmatter parsing. Does NOT perform any file I/O — callers are responsible for reading and writing files. / |
| `src/scaffold/index.ts` | Orchestrator for scaffolding a project directory. Walks the templates directory, applies substitutions, and writes files. |
| `src/scaffold/manifest.ts` | Manages the bdd-workflow write manifest at `.opencode/.bdd-workflow-manifest.json`. The manifest records the SHA-256 hash of each framework-layer file at the time it was last written by `bdd-workflow init` or `bdd-workflow update`. This enables `update` to perform a true three-way diff: if the on-disk content's hash matches the recorded hash, the file was not modified by the user and can be safely overwritten with the new template version. If the hashes differ, the user has customized the file and the update is skipped (or merged, for frontmatter-bearing files). Does NOT track user-owned files. Does NOT perform file I/O beyond reading and writing the single manifest JSON file. / |
| `src/scaffold/templates/features/support/hooks.ts` | Cucumber hooks for bdd-workflow projects. Use these to set up and tear down test fixtures. |
| `src/scaffold/templates/features/support/world.ts` | Cucumber world for bdd-workflow projects. Extend this class to add custom properties or methods available in step definitions. |
| `src/scaffold/templates/src/index.ts` | Starter module for a bdd-workflow project. Add your project description here. / |
| `src/scaffold/update.ts` | Performs the in-place update of framework-owned files in an existing project. Reads the write manifest from `.opencode/.bdd-workflow-manifest.json` to perform a three-way diff for each framework-layer file, applies frontmatter merges for agent/command files where only user-owned keys differ, and returns a structured summary. Does NOT perform git operations — those remain the user's responsibility. Does NOT modify files outside the framework layer. / |

## Features

### bdd-workflow check command (`features/check.feature`)
- Scenario: check subcommand appears in CLI help
- Scenario: check passes in a project with no type errors and no failing tests
- Scenario: check fails when tsc reports type errors
- Scenario: check fails when cucumber tests fail

### Context Generation (`features/context.feature`)
- Scenario: Happy path — generates CONTEXT.md with all five sections
- Scenario: Module summaries include JSDoc descriptions
- Scenario: Files without JSDoc comments are omitted from modules section
- Scenario: Feature summaries include feature name and scenario names
- Scenario: Public API section lists exported function signatures
- Scenario: Deterministic output — running twice produces identical content
- Scenario: Graceful empty state — no feature files
- Scenario: Config sections flag — featureSummaries disabled
- Scenario: Config sections flag — exports disabled
- Scenario: context subcommand appears in CLI help

### bdd-workflow docs command (`features/docs.feature`)
- Scenario: docs subcommand appears in CLI help
- Scenario: docs generates markdown output in a TypeScript project
- Scenario: docs command respects the --format html flag
- Scenario: docs fails gracefully when no entry point exists
- Scenario: TypeDoc errors are surfaced clearly

### Init Command (`features/init.feature`)
- Scenario: Initialize a new project in an empty directory
- Scenario: Init creates working TypeScript project
- Scenario: Init sets up working Cucumber
- Scenario: defineConfig is importable from bdd-workflow
- Scenario: Init on existing project skips existing files
- Scenario: CLI help is available
- Scenario: Init subcommand has help
- Scenario: Init does not emit compiled artifacts into user project

### bdd-workflow learn command (`features/learn.feature`)
- Scenario: learn subcommand appears in CLI help
- Scenario: learn list prints all learning entries
- Scenario: learn list shows status and issue reference
- Scenario: learn promote --dry-run prints issue content without creating issues
- Scenario: learn promote skips already-promoted learnings
- Scenario: learn promote skips learnings with status closed
- Scenario: learn promote fails gracefully when gh is not available
- Scenario: promoted learnings are not re-promoted on subsequent runs
- Scenario: learn list shows no entries gracefully when learnings directory is empty
- Scenario: parseLearningFile correctly parses all frontmatter fields

### BDD workflow agent provisioned by scaffold (`features/scaffold-bdd-workflow-agent.feature`)
- Scenario: bdd-workflow agent file exists in scaffolded project
- Scenario: bdd-workflow command file exists in scaffolded project

### Phase 2 OpenCode Skills and Commands (`features/scaffold-phase2.feature`)
- Scenario: All 3 skill files exist in scaffolded project
- Scenario: All 7 command files exist in scaffolded project
- Scenario: Review agent file exists in scaffolded project
- Scenario: All 3 template files have correct structure with frontmatter
- Scenario: Proposal template has all required sections
- Scenario: Review template has all required sections
- Scenario: Learning template has all required sections
- Scenario: Skills contain expected content
- Scenario: Commands contain expected structure
- Scenario: Review agent has permission restrictions
- Scenario: opencode.json includes review agent config
- Scenario: bdd-workflow skill references npx bdd-workflow check for the pre-review gate
- Scenario: bdd-workflow skill enforces explicit archive approval gate
- Scenario: learn command prompts for all required learning entry sections

### bdd-workflow specs command (`features/specs.feature`)
- Scenario: specs subcommand appears in CLI help
- Scenario: specs generates SPECS.md with a section for each feature
- Scenario: Each section lists all scenarios with full step text
- Scenario: Summary table is accurate
- Scenario: specs runs gracefully when there are no feature files
- Scenario: Tags are included when present on scenarios
- Scenario: Scenario Outlines are shown as outlines, not as expanded example rows
- Scenario: specs respects the --output flag

### bdd-workflow update command (`features/update.feature`)
- Scenario: Update refreshes an outdated framework file that the user has not modified
- Scenario: Update skips a file that already matches the template
- Scenario: Update adds a new framework file that did not exist
- Scenario: Update merges a file where the user has only changed the model
- Scenario: Update merges an outdated file preserving the user's model choice
- Scenario: Update skips a user-modified framework file without --force
- Scenario: Update overwrites a user-modified file when --force is given
- Scenario: Update fails when run outside an initialized project

## Public API

### `src/commands/check.ts`
- `checkCommand(): Command`

### `src/commands/context.ts`
- `contextCommand(): Command`

### `src/commands/docs.ts`
- `docsCommand(): Command`

### `src/commands/init.ts`
- `initCommand(): Command`

### `src/commands/learn.ts`
- `learnCommand(): Command`

### `src/commands/specs.ts`
- `specsCommand(): Command`

### `src/commands/update.ts`
- `updateCommand(): Command`

### `src/config.ts`
- `defineConfig(config: Partial<BddWorkflowConfig>): BddWorkflowConfig`
- `loadConfig(configPath?: string | undefined): Promise<BddWorkflowConfig>`
- `interface BddConfig`
- `interface DocsConfig`
- `interface ContextConfig`
- `interface WorkflowConfig`
- `interface BddWorkflowConfig`

### `src/generators/context.ts`
- `generateContext(config: BddWorkflowConfig): Promise<void>`

### `src/generators/docs.ts`
- `generateDocs(config: BddWorkflowConfig): Promise<void>`

### `src/generators/specs.ts`
- `generateSpecs(config: BddWorkflowConfig, outputPath: string): Promise<void>`

### `src/generators/structure.ts`
- `generateDirectoryTree(config: BddWorkflowConfig): Promise<string>`

### `src/index.ts`
- `defineConfig: (config: Partial<BddWorkflowConfig>) => BddWorkflowConfig`
- `BddWorkflowConfig: any`
- `BddConfig: any`
- `DocsConfig: any`
- `ContextConfig: any`
- `WorkflowConfig: any`

### `src/learn/index.ts`
- `listLearnings(config: BddWorkflowConfig): Promise<LearningEntry[]>`
- `parseLearningFile(filePath: string): Promise<LearningEntry>`
- `markAsPromoted(entry: LearningEntry, issueNumber: number): Promise<void>`
- `interface LearningEntry`

### `src/learn/promote.ts`
- `promoteLearnings(config: BddWorkflowConfig, opts: { dryRun: boolean; }): Promise<void>`

### `src/parsers/gherkin.ts`
- `parseFeatureFiles(config: BddWorkflowConfig): Promise<FeatureSummary[]>`
- `parseFeatureFilesDetailed(config: BddWorkflowConfig): Promise<FeatureDetail[]>`
- `interface FeatureSummary`
- `interface StepDetail`
- `interface ScenarioDetail`
- `interface FeatureDetail`

### `src/parsers/jsdoc.ts`
- `extractModuleSummaries(config: BddWorkflowConfig): Promise<ModuleSummary[]>`
- `interface ModuleSummary`

### `src/parsers/typescript.ts`
- `extractPublicApi(config: BddWorkflowConfig): Promise<ApiEntry[]>`
- `interface ApiEntry`

### `src/scaffold/frontmatter.ts`
- `hasFrontmatter(content: string): boolean`
- `mergeFrontmatter(templateContent: string, diskContent: string): string`
- `USER_OWNED_FRONTMATTER_KEYS: readonly ["model", "temperature"]`

### `src/scaffold/index.ts`
- `scaffoldProject(targetDir: string, opts?: ScaffoldOptions): Promise<void>`
- `FRAMEWORK_LAYER_GLOBS: string[]`
- `BDD_WORKFLOW_MARKER: ".opencode/skills/bdd-workflow/SKILL.md"`
- `interface ScaffoldOptions`

### `src/scaffold/manifest.ts`
- `readManifest(targetDir: string): Manifest`
- `writeManifest(targetDir: string, manifest: Manifest): void`
- `hashContent(content: string): string`
- `type Manifest`

### `src/scaffold/templates/features/support/world.ts`
- `class BddWorkflowWorld`

### `src/scaffold/templates/src/index.ts`
- `hello(name: string): string`

### `src/scaffold/update.ts`
- `updateScaffold(targetDir: string, opts?: UpdateOptions): UpdateResult`
- `printUpdateSummary(result: UpdateResult): void`
- `interface UpdateOptions`
- `interface UpdateResult`
