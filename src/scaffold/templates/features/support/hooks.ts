/**
 * Cucumber hooks for bdd-workflow projects.
 * Use these to set up and tear down test fixtures.
 */

import { Before, After } from '@cucumber/cucumber';

/**
 * Run before each scenario.
 */
Before(function () {
  // Set up fixtures here
});

/**
 * Run after each scenario.
 */
After(function () {
  // Clean up here
});
